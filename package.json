{
  "name": "night-market-cat",
  "version": "0.3.0",
  "private": true,
  "description": "跟着走，碰到就吃。一只在夜市里越吃越大的猫。",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "typescript": "5.8",
    "vite": "^6.4.3"
  }
}
